name: Greetings

on:
  issues:
    types: [opened]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.MY_TOKEN }}
        issue-message: "You issue will be assigned to GHE Support Team. We will keep you updated on this issue with comments"
        
